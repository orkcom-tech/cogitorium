schema: 1
id: gateway
name: Gateway Probe
version: 1.0.0
license: Apache-2.0
needs: python
host:
  contract: 1
pages:
  - path: /p/gateway/
    template: gateway.page.home
    title: Gateway Probe
    provider: home
